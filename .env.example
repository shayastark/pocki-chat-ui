# Privy Configuration
# Get your Privy App ID from https://dashboard.privy.io
NEXT_PUBLIC_PRIVY_APP_ID=your_privy_app_id_here

# XMTP Configuration
# Use 'production' for mainnet or 'dev' for testnet
NEXT_PUBLIC_XMTP_ENV=production

# AI Agent Configuration
# The XMTP address of the AI wallet health agent
NEXT_PUBLIC_AGENT_ADDRESS=0xd003c8136e974da7317521ef5866c250f17ad155

# Neynar Configuration (Server-side only)
# Get your Neynar API key from https://neynar.com
# Used to fetch Farcaster user profiles (username, pfp, etc.)
# NOTE: Do NOT prefix with NEXT_PUBLIC_ - this should remain server-side only
NEYNAR_API_KEY=your_neynar_api_key_here

# Alchemy Configuration (Optional - Server-side only)
# Get your Alchemy API key from https://alchemy.com
# Used for enhanced RPC reliability (optional - public RPCs are used as fallback)
# NOTE: Do NOT prefix with NEXT_PUBLIC_ - this should remain server-side only
ALCHEMY_API_KEY=your_alchemy_api_key_here
